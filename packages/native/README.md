# @tripley-acctron/native

Adapters around `@tripley-kit/native`.

Do not call container invoke APIs directly from framework or business code. Add missing native needs to `docs/native-required-capabilities.md`.
