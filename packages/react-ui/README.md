# @tripley-acctron/react-ui

React adapter for framework `UiPort` contracts.

V1 only provides a minimal runtime store and adapter. Update this document when screen rendering or store behavior changes.
