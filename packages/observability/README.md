# @tripley-acctron/observability

Structured logging helpers and `InteractionRecorder`. Customer interactions are written to both the
application logger and `JournalPort` using safe codes rather than sensitive raw input.

