# Host message profiles reference registered field codecs

Host message profiles remain declarative and may reference only built-in field behavior or a stable field codec ID registered from trusted application or plugin source. Custom wire algorithms therefore stay extensible and independently testable without embedding anonymous functions, evaluated strings, or hidden execution behavior in field declarations.
